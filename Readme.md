# Git-Hub practice

## Git-Command
`git clone <Rep Link>` 
`git add . || <file name>`  
`git commite -m "file name"`  
`git push`  
`git pull`  
`git status` 
`git remote`
`git merge`
`git rm`
`git init`
`git config`
